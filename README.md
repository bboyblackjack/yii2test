#### Для запуска приложения необхожимо выполнить консольную команду
```shell
sh deploy.sh
```

Для успешного запуска порты 80, 5432, 9000 должны быть свободны. В противном случае необходимо внести изменения в файл docker-compose.yml.